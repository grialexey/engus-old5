.header {
    background-color: #00AEEC;

    &__wrapper {
        container()
        position: relative;
    }

    &__menu {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        flex-wrap: wrap;
        width: 100%;

        &-item {
            flex: 0 0 auto;
            margin: 15px 15px 15px 0;

            &:last-child {
                margin-right: 0;
            }

            &--fill {
                flex: 1 1 auto;
                margin: 0;
            }
        }

        &-link {
            display: block;
            font-size: 18px;
            line-height: 25px;
            text-decoration: none;
            color: #fff;
            padding: 7px 14px;
            transition: background-color 0.1s, opacity 0.1s;
            cursor: pointer;

            &:hover:not(.active):not(.loading):not(&--logo) {
                background-color: #009dd9;
            }

            &.active {
                background-color: #fff;
                color: #00AEEC;
            }

            &--logo {
                font-size: 25px;
                line-height: 25px;
                text-decoration: none;
                color: #00AEEC;
                background-color: #fff;
                padding: 7px 20px;
            }

            &--add {
                border: 1px solid #fff;
                font-size: 23px;
                position: relative;
                padding: 6px 14px

                &.active {
                    background-color: #fff;
                    color: #00AEEC;
                }

                &.loading {
                    opacity: 0.7;
                    pointer: default;
                }
            }
        }
    }
}